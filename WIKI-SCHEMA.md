---
title: WIKI-SCHEMA
type: schema
updated: 2026-08-22
---

# 个人知识库 · 模式定义（WIKI-SCHEMA）

> 本文件定义知识库的结构、规范与工作流。它与使用者共同演进——随着你积累的资料变多，可以随时修订这里。
> 核心理念：不使用 RAG（每次查询都检索），而是让 LLM **持续地构建并维护一个持久、可累积的维基**（一组结构化、互相链接的 Markdown 文件）。你负责找资料、问问题；LLM 负责写与维护。

## 一、定位与架构决策

依据使用者确认的两大目标搭建：
1. **长期稳定、专属、可累积**，并能方便链接到 WorkBuddy 及任何其他系统（IMA、未来工具、静态站等）；
2. **手机随手捕获资料**，随时丢入知识库。

据此采用**两层存储架构**：
- **实时同步层（iCloud 云盘）**：知识库位于 `~/Library/Mobile Documents/com~apple~CloudDocs/个人知识库/`。承担手机捕获与多设备（Mac / iPhone / iPad）读取；Obsidian 打开此文件夹为仓库即可阅读图谱与反链。
- **耐久与可移植层（git 远端，如 GitHub）**：整个文件夹是一个 git 仓库，定期推送至远端。这是真正的备份、版本历史，也是"可被任意系统链接"的保证——任何支持 Markdown / git 的工具都能 clone 或读取。

> 为何分两层：iCloud 解决"方便"，git 远端解决"稳定 + 可链接"。iCloud 是同步而非备份，且对 `.git` 内部小文件不够友好；git 远端独立于 iCloud，提供可携带的真相源与逃生舱。

**跨系统链接原则**：
- 唯一事实来源 = **开放 Markdown + git 远端**。WorkBuddy 原生读写；Obsidian 阅读；IMA 等 RAG 工具由其成品按需导入（单向，不作为源头）。
- 不让厂商云（如 IMA）成为事实来源——否则丧失可移植与可累积性。
- "链接到其他系统" = 从该 git 仓库读取 / 导出，而非把知识锁进某一端。

## 二、三层架构

| 层 | 位置 | 谁维护 | 说明 |
|----|------|--------|------|
| **原始资料 (Raw)** | `raw/` | 你（不可修改） | 文章、论文、图片、数据文件等来源文档。**不可变**，LLM 只读不写。这是唯一事实来源。 |
| **维基 (Wiki)** | `wiki/` | LLM 全权维护 | LLM 生成的 Markdown：摘要、实体页、概念页、对比、总览、综合。你读，LLM 写。 |
| **模式 (Schema)** | `WIKI-SCHEMA.md` | 共同演进 | 本文件。定义结构、规范、工作流。 |

`raw/assets/` 存放下载的图片等附件，固定此目录以保证持久化。
`raw/inbox/` 是手机 / 随手捕获的待处理投递箱，摄入后归档至 `raw/`。

## 三、目录结构

```
个人知识库/
├── raw/                 # 原始资料（不可变）
│   ├── inbox/           # 手机/随手捕获的待处理资料（摄入后归档至 raw/）
│   ├── 佛法经典/         # 佛法经典资料存储（专属收藏）
│   ├── 自媒体/           # 自媒体资料存储（专属收藏）
│   └── assets/          # 图片、附件
├── wiki/                # LLM 维护的 Markdown 文件
│   ├── index.md         # 内容目录（每次 ingest 后更新）
│   └── log.md           # 操作日志（仅追加）
└── WIKI-SCHEMA.md       # 本文件
```

## 四、页面格式模板

每个 wiki 页面遵循统一 frontmatter：

```markdown
---
title: 页面标题
type: entity | concept | source-summary | comparison | synthesis
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: [来源文件名列表]
tags: [相关标签]
---

# 页面标题

正文。跨引用使用 [[wiki-链接]] 语法。

## 参见
- [[相关页面-1]]
- [[相关页面-2]]
```

### 页面类型

- **source-summary**：单份资料的提炼摘要，对应一次 ingest。
- **entity**：实体页（人物、组织、项目、工具等）。
- **concept**：概念/方法页（可跨多份资料累积）。
- **comparison**：对比分析（如 A vs B）。
- **synthesis**：综合/主题总览，反映你已读内容的整体认识。

## 五、跨引用规则

- 使用 `[[页面标题]]` 互链；页面标题尽量唯一、稳定。
- 新页面创建后，必须回到 `index.md` 登记，并补回所有相关页面的 `## 参见`。
- 互相矛盾的论断要**显式标注冲突**，而非静默覆盖。

## 六、分类（个人知识库默认类目）

`index.md` 按以下类目组织（可增删）：

- **项目**：投标/招标项目、工作主线、责任人分工
- **阅读与学习**：文章、书籍、课程笔记
- **想法与笔记**：零散洞察、待深挖的问题
- **人物**：同事、专家、关键联系人
- **工具与方法**：Workflow、自动化脚本、效率工具
- **佛法经典**：佛法经典资料的专属存储与维基（`raw/佛法经典/` 为原始存储，`wiki/佛法经典/` 为提炼）；随摄入累积
- **自媒体**：自媒体运营相关的选题、脚本、素材、数据、平台规则等资料（`raw/自媒体/` 为原始存储，`wiki/自媒体/` 为提炼）
- **综合**：跨领域总览与结论

## 七、工作流

### 1. Ingest（摄入）
资料有两路来源：本机直接放入 `raw/`；手机通过 iCloud 丢入 `raw/inbox/`。摄入时 LLM：
1. 读取资料（优先处理 `raw/inbox/`）并讨论要点；
2. 写一份 `source-summary` 页面；
3. 更新 `index.md`；
4. 更新相关 entity / concept 页面（一份资料可能触及 10–15 页，**建议一次摄入一份**）；
5. 在 `log.md` 追加一条记录；
6. 已处理的 `inbox/` 资料移入 `raw/` 归档（保持不可变）。

### 2. Query（查询）
你提问 → LLM：
1. 读 `index.md` 定位相关页面；
2. 读取并综合作答（带引用）；
3. **把好的答案沉淀为新页面**（对比、分析、连接关系不要只留在对话里）。

### 3. Lint（体检）
定期健康检查，查找：
- 页面间矛盾；
- 被新资料推翻的过期论断；
- 无入链的孤儿页；
- 被提及但缺独立页面的概念；
- 缺失的跨引用；
- 可通过联网检索填补的数据缺口。

### 手机摄入工作流
1. 手机上通过「文件」App 分享、或 Obsidian 移动端快速笔记，把资料丢进 `raw/inbox/`；
2. 回到 Mac，告诉 WorkBuddy「摄入 raw/inbox」；
3. LLM 读取 → 生成 `source-summary`、更新 `wiki/` 与 `index.md`；
4. 已处理资料从 `inbox/` 移入 `raw/`（归档，保持不可变）。

> 关键分工：手机只做"捕获"（写 `inbox/`），Mac / WorkBuddy 做"处理"（写 `wiki/`）。避免多设备同时写 `wiki/`，从而规避 iCloud + git 的冲突风险。

## 八、约定与提示

- **原始资料不可变**：绝不修改 `raw/` 内文件。
- **LLM 拥有维基**：你读，LLM 写与维护。
- **知识可累积**：每次摄入与查询都让维基更丰富。
- **好答案要存档**：别让有价值的分析淹没在对话里。
- **定期 Lint**：随规模增长保持维基健康。
- 整个知识库是一个 git 仓库（见第一节），git 远端提供版本历史、备份与跨系统链接能力；Obsidian 打开此文件夹为仓库即可查看图谱。
- **定期推送 git 远端**：在 Mac 上执行 `git push`，使 GitHub 等远端始终持有最新、可携带的真相源。
